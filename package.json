{
  "name": "project-scopes-v2",
  "displayName": "Project Scopes",
  "description": "Define scopes which can contain multiple globs. Each scope can be enabled or disabled. Explorer will hide files that matched any enabled scope.",
  "version": "2.0.0",
  "license": "MIT",
  "private": true,
  "publisher": "xxx",
  "icon": "icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/mikem651/vscode-project-scopes.git"
  },
  "engines": {
    "vscode": "^1.70.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished",
    "onView:scopesManager",
    "onCommand:project-scopes.addExclusionGlob",
    "onCommand:project-scopes.addExclusionPath",
    "onCommand:project-scopes.removeExclusion",
    "onCommand:project-scopes.editExclusion",
    "onCommand:project-scopes.toggle",
    "onCommand:project-scopes.toggleActivateScope",
    "onCommand:project-scopes.activateAllScopes",
    "onCommand:project-scopes.deactivateAllScopes",
    "onCommand:project-scopes.refresh",
    "onCommand:project-scopes.switcher",
    "onCommand:project-scopes.delete",
    "onCommand:project-scopes.add"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "views": {
      "explorer": [
        {
          "id": "scopesManager",
          "name": "Project Scopes"
        }
      ]
    },
    "commands": [
      {
        "command": "project-scopes.add",
        "title": "Add a new project scope and activate it",
        "category": "Project Scopes",
        "icon": "$(add)"
      },
      {
        "command": "project-scopes.delete",
        "title": "Delete project scope",
        "category": "Project Scopes"
      },
      {
        "command": "project-scopes.switcher",
        "title": "Toggle project scope",
        "category": "Project Scopes"
      },
      {
        "command": "project-scopes.refresh",
        "title": "Refresh project scopes settings",
        "category": "Project Scopes"
      },
      {
        "command": "project-scopes.toggle",
        "title": "Toggle project scopes extension",
        "category": "Project Scopes"
      },
      {
        "command": "project-scopes.toggleActivateScope",
        "title": "Toggle project scope activation",
        "category": "Project Scopes"
      },
      {
        "command": "project-scopes.activateAllScopes",
        "title": "Activate all project scopes",
        "category": "Project Scopes",
        "icon": "$(check-all)"
      },
      {
        "command": "project-scopes.deactivateAllScopes",
        "title": "Deactivate all project scopes",
        "category": "Project Scopes",
        "icon": "$(clear-all)"
      },
      {
        "command": "project-scopes.addExclusionPath",
        "title": "Project Scopes: Hide path",
        "category": "Project Scopes"
      },
      {
        "command": "project-scopes.removeExclusion",
        "title": "Project Scopes: Don't hide path",
        "category": "Project Scopes"
      },
      {
        "command": "project-scopes.editExclusion",
        "title": "Project Scopes: Edit glob",
        "category": "Project Scopes"
      },
      {
        "command": "project-scopes.addExclusionGlob",
        "title": "Add glob",
        "category": "Project Scopes"
      }
    ],
    "menus": {
      "commandPalette": [
        {
          "command": "project-scopes.addExclusionPath",
          "when": "false"
        },
        {
          "command": "project-scopes.removeExclusion",
          "when": "false"
        },
        {
          "command": "project-scopes.editExclusion",
          "when": "false"
        },
        {
          "command": "project-scopes.toggleActivateScope",
          "when": "false"
        }
      ],
      "editor/title/context": [
        {
          "command": "project-scopes.addExclusionPath",
          "group": "Project Scopes"
        },
        {
          "command": "project-scopes.removeExclusion",
          "group": "Project Scopes"
        }
      ],
      "explorer/context": [
        {
          "command": "project-scopes.addExclusionPath",
          "group": "Project Scopes"
        },
        {
          "command": "project-scopes.removeExclusion",
          "group": "Project Scopes"
        }
      ],
      "view/item/context": [
        {
          "command": "project-scopes.addExclusionGlob",
          "group": "Project Scopes",
          "when": "viewItem == 'scope'"
        },
        {
          "command": "project-scopes.delete",
          "group": "Project Scopes",
          "when": "viewItem == 'scope'"
        },
        {
          "command": "project-scopes.removeExclusion",
          "group": "Project Scopes",
          "when": "viewItem == 'exclusion'"
        },
        {
          "command": "project-scopes.editExclusion",
          "group": "Project Scopes",
          "when": "viewItem == 'exclusion'"
        }
      ],
      "view/title": [
        {
          "command": "project-scopes.activateAllScopes",
          "group": "navigation@1"
        },
        {
          "command": "project-scopes.deactivateAllScopes",
          "group": "navigation@2"
        },
        {
          "command": "project-scopes.add",
          "group": "navigation@3"
        }
      ]
    },
    "configuration": {
      "title": "Project Scopes",
      "properties": {
        "project-scopes.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Whether or not to show/hide files based on scope selection"
        },
        "project-scopes.activeScopes": {
          "type": "array",
          "default": [],
          "description": "Scopes currently in use by the Scopes extension"
        },
        "project-scopes.globalExclude": {
          "type": "object",
          "default": null,
          "description": "Globals to merge in with scope settings into files.exclude"
        },
        "project-scopes.scopes": {
          "type": "object",
          "default": {
            "base": {
              "excluded": []
            }
          },
          "description": "Exclusion settings per scope"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "yarn run compile",
    "compile": "npx tsc -p ./",
    "watch": "npx tsc -watch -p ./",
    "pretest": "yarn run compile && yarn run lint",
    "lint": "eslint src --ext ts",
    "test": "node ./out/test/runTest.js"
  },
  "dependencies": {
  },
  "devDependencies": {
    "@types/mocha": "^10.0.0",
    "@types/node": "16.x",
    "@types/vscode": "1.70.0",
    "@typescript-eslint/eslint-plugin": "^5.38.1",
    "@typescript-eslint/parser": "^5.38.1",
    "@vscode/test-electron": "^2.1.5",
    "eslint": "^8.24.0",
    "mocha": "^10.0.0",
    "typescript": "^4.9.5"
  }
}
